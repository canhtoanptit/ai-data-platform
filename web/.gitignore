# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# pnpm's content-addressable store, if it is ever pointed at the project dir.
# pnpm-lock.yaml and pnpm-workspace.yaml are committed on purpose — the lockfile
# is the build's input and the yaml holds the install-security settings.
.pnpm-store/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
